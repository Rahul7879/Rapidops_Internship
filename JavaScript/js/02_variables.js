// A variable is a “named storage” for data.
//  We can use variables to store goodies, visitors, and other data.

let a = "Hello World";
a = "Hello Rahul";

// We can also declare multiple variables in one line:
// but variable type should be same

let b = 10, c=20, d= 30;
console.log(c);

const arr = [1,2,3,4];
arr[0] = 10;
console.log(arr[0])
